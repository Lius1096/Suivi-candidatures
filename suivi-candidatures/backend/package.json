{
  "name": "suivi-candidature",
  "version": "1.0.0",
  "description": "Application de suivi de candidatures avec Node.js, Express, MongoDB et Tailwind CSS",
  "main": "app.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "dev": "nodemon app.js",
    "start": "node app.js",
    "build:css": "tailwindcss -i ./public/input.css -o ./public/styles.css",
    "watch:css": "tailwindcss -i ./public/input.css -o ./public/styles.css --watch",
    "test": "echo \"No test yet\" && exit 0",
    "tailwind:build": "tailwindcss -i ./public/input.css -o ./public/styles.css --watch"
  },
  "author": "Ton Nom",
  "license": "ISC",
  "dependencies": {
    "@tailwindcss/cli": "^4.1.11",
    "bcrypt": "^5.1.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-validator": "^7.2.1",
    "mongoose": "^7.6.0",
    "multer": "^1.4.5-lts.1",
    "node-cron": "^3.0.3",
    "nodemailer": "^6.9.7"
  },
  "devDependencies": {
    "nodemon": "^3.1.0",
    "tailwindcss": "^3.4.3"
  }
}
